name: Killer Whale Backtests

on:
  workflow_dispatch:
  push:

jobs:
  backtest:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install deps
      run: |
        pip install -r requirements.txt || true

    - name: Run backtest
      run: |
        echo "Running backtest..."
        # Real backtesting logic will be added later
