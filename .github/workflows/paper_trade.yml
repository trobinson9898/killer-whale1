name: Killer Whale Paper Trading Smoke Test

on:
  workflow_dispatch:
  push:

jobs:
  paper:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install requirements
      run: |
        pip install -r requirements.txt || true

    - name: Run paper trading test
      env:
        DRY_RUN: "true"
      run: |
        echo "Starting paper trading smoke test..."
